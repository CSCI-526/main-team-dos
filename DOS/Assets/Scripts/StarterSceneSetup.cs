using UnityEngine;

public class StarterSceneSetup : MonoBehaviour
{
    void Start()
    {
        // Show mouse again
        Cursor.visible = true;
        Cursor.lockState = CursorLockMode.None;
    }
}
